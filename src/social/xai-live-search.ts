/**
 * xAI Responses API — mars 2026 : `search_parameters` / ancien **x_search** côté client → **410 Gone**.
 * L’outil officiel **`web_search`** couvre recherche web + X en direct (Narrative Radar).
 */
export const XAI_RESPONSES_WEB_TOOLS = [{ type: 'web_search' as const }];
